apiVersion: 1

providers:
  - name: moderation-service
    folder: Moderation Service
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
